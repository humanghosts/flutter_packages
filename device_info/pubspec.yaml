name: hgs_device_info
description: A new Flutter project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  hgs_framework:
    git:
      url: https://github.com/humanghosts/flutter_packages.git
      path: framework
  hgs_prefs:
    git:
      url: https://github.com/humanghosts/flutter_packages.git
      path: prefs

  ## 设备信息
  device_info_plus: ^4.1.3
  ## 解析浏览器userAgent 用于web端设备信息
  user_agent_analyzer: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

